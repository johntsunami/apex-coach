// Stub — workout building is in App.jsx. This provides a minimal export
// for components that need access outside the main app tree.
export function buildWorkoutList() { return { warmup: [], main: [], cooldown: [], all: [] }; }
